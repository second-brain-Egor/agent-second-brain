---
type: note
description: "Forumhouse: авто-реестр выгрузок + дельта-докачка — как работает и как отвечать на вопросы о перекачке"
last_accessed: 2026-06-11
relevance: 0.74
tier: warm
---
# Forumhouse: авто-реестр выгрузок и дельты (с 2026-06-11)

Канон по `ssh barriga` → `/root/forum-harvest`. Полные правила: `/root/forum-harvest/ПРАВИЛА-ВЫГРУЗКИ.md`.

## Суть

- Реестр выгруженных тем: `runtime/exported_threads.json`. Фаза `register` в конце
  каждого цикла фиксирует ВСЕ темы с диска (включая partial) со свежими счётчиками.
- Целевой инвариант: завершённая тема фиксируется в независимом реестре сразу после
  загрузки; удаление каталога темы после выгрузки не должно влиять на её статус и
  никогда не должно возвращать её в полную загрузку.
- Егор копирует **всю папку раздела целиком** и удаляет с сервера — ручных шагов нет.
- Раз в 8 циклов (~сутки) audit пересматривает живой форум (`--refresh-audit-every-cycles 8`):
  находит новые темы и рост существующих.
- Выросшая тема: файл на диске → докачка в файл; файл выгружен → качается **только хвост**
  в новый файл с `posts_offset` (полная перекачка не происходит никогда).
- 475 тем майского ручного импорта (999999) закрыты навсегда — нет точки возобновления.

## Как проверять «не качает ли по кругу»

1. `runtime/summary.json` → `status_counts`: `exported_pruned` — выгружено и не трогается,
   `delta_pending` — ждут докачки хвоста (это НЕ перекачка).
2. В `runtime/events.jsonl` фазы `register` и `audit` — `returncode 0`.
3. В логе sync/repair у дельт строка «качаю только хвост со стр. N».

## Требование после очистки 2026-08-10

- Сверить все выгруженные и удалённые в этот день темы с независимым реестром.
- Проверить, что запись появляется автоматически при завершении каждой темы, а не
  зависит от наличия её файлов на сервере или от поздней пакетной регистрации.
- До подтверждения этой схемы не считать защиту от повторной загрузки доказанной.

## Связанное

- [[loading-vs-git-export]] — различение «загрузка Forumhouse» vs «git push».
- Известный фон: sync-фаза периодически падает на DDoS-Guard (антибот) — новые темы
  и дельты качаются в удачные окна. Это не ошибка реестра.

## Живая сверка перед удалением папок 11 сентября 2026, 09:58 МСК

- Загрузчик остановлен; в ходе проверки загрузка не запускалась.
- В независимом реестре 3468 тем. Все 684 оставшихся JSON темы раздела 91
  зарегистрированы, счётчики не отстают; повреждённых JSON не обнаружено.
  Из этих файлов 583 завершены, 101 частично скачан.
- У 2784 зарегистрированных тем JSON уже отсутствует. Все идентификаторы из
  `progress.scraped_threads` и 649 тем с завершённой обработкой в сентябрьском
  журнале присутствуют в реестре. Незарегистрированных папок вложений нет.
- Проверка штатной функцией `inspect_thread_file` с отсутствующим путём для
  каждой записи реестра: 3367 `exported_pruned`, 101 `delta_pending`, ни одной
  `missing`. У всех 101 записей для докачки есть сохранённая страница и число
  сообщений: удаление JSON не переводит их в полную загрузку.
- По индексу раздела из 4408 тем после условного удаления: 3366 уже выгружены,
  101 ждёт докачки, 941 отсутствует в реестре. Ещё одна запись реестра вне индекса.
- Подтверждено по действующему коду: сразу после записи результата темы
  `register_thread_immediately` фиксирует её в отдельном реестре с блокировкой
  и атомарной заменой файла. Пакетную перерегистрацию не запускали: данные актуальны.
- Проверенная резервная копия реестра, прогресса, индекса и аудита:
  `/root/forum-harvest/runtime/registry-before-folder-cleanup-20260911T065812Z.tar.gz`.
  Подробный отчёт: `/root/forum-harvest/runtime/registry-check-20260911T065812Z.json`.
- При очистке скачанного раздела сохранить `/root/forum-harvest/runtime/`:
  там независимый реестр и его резервная копия. Копия на компьютере пользователя
  этой проверкой не проверялась.
