+ Reading book fling each chapter as you go, buldng out pages for characters themes, plot treads and how thay

‘connect. By the end you have a rich companion wiki. Thnk of fan wikis lke Tolkien Gateway — thousands of interlinked

‘pages covering characters. places, events, languages, built by @ community of volunteers over years. You could build

‘something lk that personally as you read, with the LLM doing all the cross-referencing and maintenance.

‘+ Basiness/team: an intemal wiki maintained by LLMs, fed by Slack threads, meeting transcripts, project documents,

‘customer calls. Possibly with humans in the loop reviewing updates. The wiki stays current because the LLM does the

‘maintenance that no one on the team wants to do.

+ Competitive analysis, due diligence, trip planning, course notes, hobby deep-dives — anything where you're

‘accumulating knowledge overtime and want it organized rather than scattered.

Architecture

‘There are three layers:

Raw sources — your curated collection of source documents. Articles, papers, images, data files. These are immutable — the

LLM reads from them but never modifies them. This is your source of truth.

‘The wiki —a directory of LLM-generated markclown files. Summaries, entity pages, concept pages, comparisons, an overview,

synthesis. The LLM owns this ayer entirely. It creates pages, updates them when new sources arrive, maintains cross-

references, and keeps everything consistent. You read it; the LLM writes it.

‘The schema — a document (eg. CLAUDEmd for Claude Code or AGENTS:md for Codes) that tells the LLM how the wikis

structured, what the conventions are, and what workflows to follow when ingesting sources, answering questions, oF

‘maintaining the wiki. This isthe key configuration file — it's what makes the LLM a disciplined wiki maintainer rather than a

‘generic chatbot. You and the LLM co-evolve this overtime as you figure out what works for your domain.

AIS

Operations

Ingest. You drop @ new source into the raw collection and tell the LLM to process it An example flow: the LLM reads the source,

discusses key takeaways with you writes a summary page in the wiki, updates the index, updates relevant entity and concept

pages across the wiki. and appends an entry tothe log. single source might touch 10-15 wiki pages. Personally I prefer to

ingest sources one at time and stay involved — | read the summaries, check the updates, and guide the LLM on what to

‘emphasize. But you could also batch ingest many sources at once with less supervision It's up to you to develop the workdlow

that fits your style and document iti the schema for future sessions.

‘Query. You ack questions against the wiki. The LIM searches for relevant pages, reads them, and synthesizes an answer with
